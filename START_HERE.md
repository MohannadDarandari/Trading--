# 🚀 تعليمات التثبيت والتشغيل - Quick Start

## ✅ الخطوات المطلوبة:

### 1️⃣ تثبيت Python (مرة واحدة فقط)

```
1. حمل Python من:
   https://www.python.org/downloads/

2. أثناء التثبيت:
   ✅ ضع علامة على: "Add Python to PATH"
   ⚠️ هذا مهم جداً!

3. أكمل التثبيت

4. أعد فتح PowerShell
```

---

### 2️⃣ تشغيل التثبيت التلقائي

بعد تثبيت Python، شغل:

```powershell
.\INSTALL.ps1
```

هذا السكريبت سيعمل:
- ✅ فحص Python
- ✅ إنشاء ملف .env
- ✅ تثبيت جميع المكتبات
- ✅ تثبيت الواجهة (UI)
- ✅ اختبار النظام

---

### 3️⃣ تعديل الإعدادات

افتح ملف `.env` وعدل:

```powershell
notepad .env
```

**المطلوب تعديله:**
```
PRIVATE_KEY=your_private_key_here    # مفتاح محفظة Polygon
RPC_URL=your_alchemy_url_here        # احصل عليه من alchemy.com
MAX_POSITION_SIZE=10                 # ابدأ بمبلغ صغير
DRY_RUN=true                         # للتجربة بدون مخاطرة
```

---

### 4️⃣ تشغيل الواجهة (Dashboard)

```powershell
streamlit run dashboard.py
```

ستفتح صفحة على: `http://localhost:8501`

---

## 🎨 مميزات الواجهة:

### 📊 **Dashboard - الصفحة الرئيسية**
- إحصائيات الأداء
- الصفقات والأرباح
- أزرار سريعة

### 🔍 **Market Scanner**  
- عرض الأسواق النشطة
- فلترة حسب الحجم
- تحليل متقدم لكل سوق

### 💎 **Opportunities Finder**
- بحث عن أفضل الفرص
- فلترة حسب الثقة والاستراتيجية
- تفاصيل وتحليل كامل

### ⚙️ **Settings**
- تحكم في حجم الصفقات
- إعدادات المخاطر
- Stop Loss & Take Profit

---

## 🤖 تشغيل البوت بدون واجهة:

```powershell
# تجربة بدون تداول حقيقي:
python agent/trader_advanced.py --dry-run

# تداول حقيقي (بعد التأكد):
python agent/trader_advanced.py
```

---

## ⚡ أوامر مفيدة:

```powershell
# فتح الإعدادات
notepad .env

# تشغيل الواجهة
streamlit run dashboard.py

# فحص Python
python --version

# تحديث المكتبات
pip install -r requirements.txt --upgrade

# مسح الكاش
streamlit cache clear
```

---

## 🚨 مشاكل شائعة:

### ❌ "Python was not found"
```
الحل: ثبت Python مع تفعيل "Add to PATH"
```

### ❌ "Command 'streamlit' not found"
```
الحل: pip install streamlit
```

### ❌ Dashboard لا يفتح
```
الحل: 
1. تأكد من البورت 8501 مش مستخدم
2. جرب: streamlit run dashboard.py --server.port 8502
```

### ❌ "ModuleNotFoundError"
```
الحل: pip install -r requirements.txt
```

---

## 📊 ترتيب البدء الصحيح:

```
1. تثبيت Python ✅
2. تشغيل INSTALL.ps1 ✅
3. تعديل .env ✅
4. تشغيل Dashboard ✅
5. اختبار لمدة أسبوع (DRY_RUN=true) ✅
6. تداول حقيقي (DRY_RUN=false) ✅
```

---

## 🎯 ملفات مهمة:

```
📁 Project/
├── 📄 INSTALL.ps1              → سكريبت التثبيت
├── 📄 dashboard.py             → الواجهة الرئيسية
├── 📄 .env                     → الإعدادات (عدلها!)
├── 📄 requirements.txt         → المكتبات المطلوبة
├── 📄 DASHBOARD_GUIDE.md       → دليل الواجهة
├── 📄 ARABIC_GUIDE.md          → الدليل الكامل
├── 📄 MASTER_PLAN.md           → الخطة الاستراتيجية
└── 📁 agent/
    ├── trader_advanced.py      → البوت المتقدم
    ├── advanced_analyzer.py    → المحلل الذكي
    └── advanced_strategies.py  → الاستراتيجيات
```

---

## 💡 نصائح:

1. **ابدأ بـ DRY_RUN**: جرب أسبوع كامل بدون تداول حقيقي
2. **استخدم محفظة جديدة**: لا تستخدم محفظتك الرئيسية
3. **ابدأ بمبلغ صغير**: $50-$100 للبداية
4. **راقب النتائج**: استخدم Dashboard لمتابعة الأداء
5. **اقرأ SECURITY.md**: مهم جداً للأمان

---

## 🎨 الآن:

```powershell
#  ثبت Python أولاً، ثم:
.\INSTALL.ps1
```

**بعد التثبيت:**
```powershell
streamlit run dashboard.py
```

---

**✨ استمتع بالتداول الآلي مع واجهة احترافية!**

*v2.0 - Complete System with UI*
