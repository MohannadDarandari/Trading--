# 🎓 الشرح الكامل بالعربي | Complete Arabic Guide

## 🤔 وش هو المشروع هذا؟

هذا **AI agent** للتداول الآلي في **Polymarket** - وهو سوق توقعات (prediction market) لامركزي.

### المفهوم البسيط:
```
1. تتوقع إذا حدث راح يصير أو لا
2. تشتري YES إذا تتوقع يصير، أو NO إذا تتوقع ما يصير
3. إذا توقعك صح، كل سهم يصير قيمته $1.00
4. الفرق بين سعر الشراء و $1.00 = ربحك
```

### مثال حقيقي:
```
السؤال: "هل Bitcoin يوصل $100k في 2026؟"

- سعر YES الحين: $0.35 (35 سنت)
- انت تشتري 100 سهم YES = $35
- إذا صار Bitcoin $100k في 2026:
  → كل سهم يصير $1.00
  → 100 × $1.00 = $100
  → ربحك = $100 - $35 = $65 ربح 💰
```

---

## 🏗️ البنية | Structure

```
Trading$$/
│
├── 📁 agent/                    # كود البوت الرئيسي
│   ├── trader.py               # البوت (القلب النابض)
│   ├── analyzer.py             # المحلل (يحلل الأسواق)
│   └── strategies.py           # الاستراتيجيات
│
├── 📁 config/                   # الإعدادات
│   └── settings.py             # كل الإعدادات هنا
│
├── 📁 scripts/                  # أدوات مساعدة
│   ├── utils.py                # أدوات عامة
│   └── monitor.py              # مراقب الأسواق
│
├── 📁 data/                     # البيانات والسجلات
│   ├── logs/                   # سجلات الأخطاء
│   ├── markets/                # بيانات الأسواق
│   └── trades/                 # سجل الصفقات
│
├── 📄 .env                      # مفاتيحك السرية (لا تشاركها!)
├── 📄 requirements.txt         # المكتبات المطلوبة
├── 📄 README.md                # الشرح الرئيسي
├── 📄 QUICKSTART.md            # دليل البدء السريع
└── 📄 SECURITY.md              # دليل الأمان
```

---

## 🧠 كيف يشتغل البوت؟

### الخطوات:

```
1. 🔍 يسكان الأسواق (يدور أسواق نشطة)
2. 📊 يحلل كل سوق (يشوف السعر، الحجم، السيولة)
3. 🎯 يعطي score (نسبة ثقة من 0-100%)
4. 💰 يقرر: يدخل أو لا؟
5. 📈 إذا قرر يدخل: يحدد حجم الصفقة
6. ✅ ينفذ الصفقة (يشتري YES أو NO)
7. 👁️ يراقب الصفقة
8. 🚪 يخرج إذا:
   - ربح كافي (take profit)
   - خسارة كبيرة (stop loss)
```

### مخطط Flow:
```
     ┌─────────────┐
     │ Start Bot   │
     └──────┬──────┘
            │
            v
     ┌──────────────┐
     │ Get Markets  │ ← API call
     └──────┬───────┘
            │
            v
     ┌──────────────┐
     │ Analyze Each │
     │   Market     │
     └──────┬───────┘
            │
            v
     ┌──────────────┐
  ┌─►│ Confidence   │
  │  │   >= 70%?    │
  │  └──────┬───────┘
  │         │
  │    Yes  │  No
  │         v    └─────► Skip
  │  ┌──────────────┐
  │  │ Check Limits │
  │  │ (daily max)  │
  │  └──────┬───────┘
  │         │
  │         v
  │  ┌──────────────┐
  │  │ Execute      │
  │  │ Trade        │
  │  └──────┬───────┘
  │         │
  │         v
  │  ┌──────────────┐
  │  │ Monitor P&L  │
  │  └──────┬───────┘
  │         │
  │         v
  │  ┌──────────────┐
  │  │ Stop Loss /  │
  │  │ Take Profit? │
  │  └──────┬───────┘
  │         │
  │    Yes  │  No
  │         v    └─────┐
  │  ┌──────────────┐  │
  │  │ Close        │  │
  │  │ Position     │  │
  │  └──────────────┘  │
  │                    │
  └────────────────────┘
         │
         v
     Wait 60s
         │
         └─────► Repeat
```

---

## 🎯 الاستراتيجيات المتاحة

### 1️⃣ Copy Whales (نسخ الحيتان) 🐋

**الفكرة:**
- يتابع محافظ كبيرة (حيتان)
- إذا حوت دخل صفقة، البوت يدخل نفس الصفقة

**متى يستخدم:**
- للمبتدئين
- إذا ما عندك خبرة في التحليل
- تبغى تتعلم من الخبراء

**مثال:**
```python
# حوت عنده $100k دخل في:
# "Will Trump win 2024?"
# البوت يشوف ويدخل نفس الصفقة
```

**الإعدادات:**
```bash
# في .env
STRATEGY=copy_whales
WHALE_WALLETS=0x123...,0x456...
MIN_WHALE_TRADE_SIZE=100
```

### 2️⃣ Arbitrage (المراجحة) ⚖️

**الفكرة:**
- يبحث عن أسواق مسعرة بشكل خاطئ
- يستغل الفروق في الأسعار

**متى يستخدم:**
- لو عندك execuation سريع
- تبغى مخاطر أقل
- تحب الأرباح الصغيرة المتكررة

**مثال:**
```
السوق: "Will it rain tomorrow?"
YES = $0.60
NO = $0.50

المجموع = $1.10 (المفروض $1.00!)

الفرصة: اشتري NO بـ $0.50
إذا ما طاح مطر → تربح $0.50
```

**الإعدادات:**
```bash
# في .env
STRATEGY=arbitrage
MIN_CONFIDENCE=0.6
```

### 3️⃣ Momentum (الزخم) 📈

**الفكرة:**
- يتبع الـ trend (الاتجاه)
- يدخل في الأسواق النشطة

**متى يستخدم:**
- لو تحب تتبع الاتجاه
- في أحداث كبيرة (انتخابات، رياضة)
- تبغى تركب الموجة

**مثال:**
```
السوق: "Will Bitcoin hit $100k?"
Volume: $50,000 (نشط جداً!)
Trend: السعر طالع من $0.30 → $0.50

البوت: "فيه momentum! أدخل"
```

**الإعدادات:**
```bash
# في .env
STRATEGY=momentum
MIN_CONFIDENCE=0.5
```

### 4️⃣ Manual (يدوي) 🎮

**الفكرة:**
- البوت يعطيك توصيات فقط
- انت تقرر متى تدخل

**متى يستخدم:**
- تبغى تتعلم
- ما تثق في البوت 100%
- تحب التحكم الكامل

---

## 📊 كيف البوت يحلل؟

### معايير التحليل:

```python
1. 📉 سعر منخفض جداً (< $0.30)
   → فرصة! (potential upside)
   → +30% confidence

2. 📈 سعر عالي جداً (> $0.70)
   → فكر في NO
   → +30% confidence

3. 💰 حجم تداول كبير (> $10k)
   → السوق نشط
   → +20% confidence

4. 💧 سيولة جيدة (> $5k)
   → سهل تبيع
   → +20% confidence

5. ⚖️ خلل في السعر
   → YES + NO ≠ $1.00
   → فرصة arbitrage!
   → +30% confidence
```

### مثال تحليل:

```
السوق: "Will Bitcoin reach $100k in 2026?"

📊 البيانات:
- YES price: $0.35
- Volume: $15,000
- Liquidity: $8,000
- YES + NO = $1.08 (!)

🧮 الحساب:
+ 30% (سعر منخفض)
+ 20% (حجم جيد)
+ 20% (سيولة جيدة)
+ 30% (خلل سعري)
─────────────────
= 100% confidence!

✅ Decision: BUY YES
💰 Position: $50
```

---

## 💰 إدارة المخاطر (Risk Management)

### القواعد الذهبية:

```
1. 🎯 1% Rule
   لا تخاطر بأكثر من 1% من رأس مالك في صفقة واحدة
   
   مثال:
   رأس المال = $1000
   صفقة واحدة = $10 max

2. 🛑 Stop Loss
   أوقف الخسارة عند -20%
   
   مثال:
   دخلت بـ $50
   وصلت خسارة $-10 (20%)
   → البوت يبيع تلقائي

3. 🎯 Take Profit
   اجني الأرباح عند +50%
   
   مثال:
   دخلت بـ $50
   وصل ربح $+25 (50%)
   → البوت يبيع تلقائي

4. 📊 Daily Limit
   لا تتداول أكثر من X صفقات في اليوم
   
   مثال:
   MAX_DAILY_TRADES = 10
   بعد 10 صفقات → البوت يوقف
```

### الإعدادات في `.env`:

```bash
MAX_POSITION_SIZE=50      # حجم أقصى للصفقة
MAX_DAILY_TRADES=10       # عدد صفقات يومياً
STOP_LOSS=0.2            # وقف خسارة 20%
TAKE_PROFIT=0.5          # جني أرباح 50%
MIN_CONFIDENCE=0.7        # حد أدنى للثقة 70%
```

---

## 🚀 خطوات التشغيل الكاملة

### الإعداد الأول (مرة واحدة):

```powershell
# 1. ثبت Python
# تأكد من تثبيت Python 3.10+
python --version

# 2. ثبت المكتبات
pip install -r requirements.txt

# 3. انسخ ملف الإعدادات
copy .env.example .env

# 4. عدّل .env
notepad .env
# أضف: PRIVATE_KEY, RPC_URL

# 5. تحقق من الإعداد
python scripts/utils.py check
```

### التشغيل اليومي:

```powershell
# A. شوف الأسواق أولاً
python scripts/monitor.py

# B. ابحث عن فرص
python scripts/utils.py opportunities

# C. اختبر استراتيجية
python scripts/utils.py test-strategy --strategy copy_whales

# D. شغل البوت (dry run)
python agent/trader.py --strategy copy_whales --dry-run

# E. تداول حقيقي (بعد ما تطمن!)
python agent/trader.py --strategy arbitrage
```

---

## 🔧 الأدوات المساعدة

### 1. Utils (أدوات عامة)

```powershell
# تحقق من الإعداد
python scripts/utils.py check

# شوف الأسواق النشطة
python scripts/utils.py markets

# ابحث عن فرص
python scripts/utils.py opportunities

# اختبر استراتيجية
python scripts/utils.py test-strategy --strategy copy_whales

# شوف رصيد المحفظة
python scripts/utils.py balance
```

### 2. Monitor (المراقب)

```powershell
# راقب الأسواق live
python scripts/monitor.py

# راقب كل 60 ثانية
python scripts/monitor.py --interval 60
```

---

## 🎓 أمثلة عملية

### سيناريو 1: مبتدئ يبغى يتعلم

```powershell
# 1. افتح المراقب
python scripts/monitor.py

# 2. شوف الفرص
python scripts/utils.py opportunities

# 3. جرب manual strategy
python agent/trader.py --strategy manual --dry-run

# 4. راقب القرارات وتعلم
```

### سيناريو 2: متوسط يبغى يختبر

```powershell
# 1. اختبر copy_whales
python scripts/utils.py test-strategy --strategy copy_whales

# 2. شغل dry run لأسبوع
python agent/trader.py --strategy copy_whales --dry-run

# 3. راجع النتائج في data/trades/

# 4. إذا النتائج جيدة، جرب live بمبلغ صغير
python agent/trader.py --strategy copy_whales
```

### سيناريو 3: متقدم يبغى يربح

```powershell
# 1. شغل arbitrage (أقل مخاطر)
python agent/trader.py --strategy arbitrage

# 2. ضبط الإعدادات في .env
MAX_POSITION_SIZE=100
MIN_CONFIDENCE=0.8

# 3. راقب live
# افتح terminal جديد
python scripts/monitor.py

# 4. تابع الأرباح في data/trades/
```

---

## ❓ FAQ (أسئلة شائعة)

### س: كم أحتاج فلوس للبداية؟
**ج:** ابدأ بـ $10-$50 للتجربة. لا تحط كل أموالك!

### س: هل أقدر أربح $10 → $4000؟
**ج:** ممكن، لكن نادر جداً. معظم الناس ما يربحون بهالطريقة. كن واقعي.

### س: وش أحسن استراتيجية؟
**ج:** للمبتدئين: manual أو copy_whales
للمتقدمين: arbitrage أو momentum

### س: هل البوت مضمون؟
**ج:** لا! مافي شيء مضمون في التداول. قد تخسر.

### س: كم ساعة يشتغل البوت؟
**ج:** 24/7 إذا تبغى. لكن موصى تراقبه باستمرار.

### س: هل يحتاج VPS؟
**ج:** مو ضروري للبداية. جهازك يكفي.

### س: وش الفرق بين dry-run والتداول الحقيقي؟
**ج:** dry-run = تجريبي (بدون فلوس)
live = حقيقي (فلوس حقيقية!)

---

## ⚠️ تحذيرات نهائية

```
🚫 لا تستثمر فلوس ما تقدر تخسرها
🚫 لا تستخدم محفظتك الرئيسية
🚫 لا تشارك مفاتيحك الخاصة
🚫 لا تثق بالبوت 100%
🚫 لا تتوقع أرباح خيالية

✅ ابدأ صغير
✅ تعلم وجرب
✅ راقب باستمرار
✅ كن مستعد للخسارة
✅ استمتع بالتعلم
```

---

## 📞 محتاج مساعدة؟

1. راجع [QUICKSTART.md](QUICKSTART.md)
2. راجع [SECURITY.md](SECURITY.md)
3. راجع `data/logs/` للأخطاء
4. استخدم `python scripts/utils.py check`

---

**بالتوفيق! 🚀**

*تذكر: التداول فن وعلم. خذ وقتك في التعلم.*

```
Made with ❤️ for Arabic traders
```
